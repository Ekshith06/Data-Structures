#122010322044
#ekshith.kolla
#Delete the minimum number of characters such that there are no consecutive "M" and no consecutive "L"
str = input("Given String")

res = str[0]

for i in range(1,len(str)):#to caluclate the range up to length string 
    if res[-1] == str[i] :
        pass
    else:
        res += str[i]
        
print(res)
# input S= "MMMLMLL" then output is "MLML"
#for consective Given a string S, consisting of "M"s and "L"s
